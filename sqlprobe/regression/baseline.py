"""Baseline regression stub.

Baseline creation and comparison are planned for a future release. Regression
logic is outside the v0.0.1 scope, so these functions preserve the future API
shape without implementing persistence or comparison behavior.
"""

from __future__ import annotations

from typing import Any


def create_baseline(*args: Any, **kwargs: Any) -> None:
    """Create a baseline in a future release."""
    raise NotImplementedError(
        "Baseline comparison is planned for a future release."
    )


def compare_baseline(*args: Any, **kwargs: Any) -> None:
    """Compare results with a baseline in a future release."""
    raise NotImplementedError(
        "Baseline comparison is planned for a future release."
    )
